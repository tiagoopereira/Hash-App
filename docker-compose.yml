version: "3.7"
services:
    php:
        build:
            context: .
        image: php
        container_name: hash_app
        ports:
            - "8080:80"
        volumes:
            - ".:/var/www/html/"
        command: php -S 0.0.0.0:80 -t public/
